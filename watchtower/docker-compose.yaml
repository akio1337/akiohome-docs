version: "3"
services:
  watchtower:
    image: containrrr/watchtower
    container_name: watchtower
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - prometheus-net
    ports:
      - 9300:8080
    env_file: .env
networks:
  prometheus-net:
    external: true